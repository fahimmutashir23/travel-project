import { useEffect } from "react";
import useAxiosPublic from "../../Hooks/useAxiosPublic";

const Pdf = () => {
    // const axiosPublic = useAxiosPublic()

    // useEffect(() => {
    //     axiosPublic.post('/generatePdf', {name: 'fahim'})
    //     .then(res => {
    //         console.log(res.data);
    //     })
    // }, [axiosPublic])
    return (
        <div>

        </div>
    );
};

export default Pdf;